1. Explain the Details fields: Header, MetaDesc, IntroText, MetaTitle, SubHeader, GetThereText, AdditionalText.
2. Verify all the different types of date.
3. Let's define our event categories
4, Should we add the following properties to agents? "Tax": "01125780211", "Vat": "82010080214",

ISSUES:
1. How should we handle objects that do not exist in a server's database?
2. What should we do about empty fields? { name: { ita: null, eng: null, deu: null} }?
3. We must always add IDs
4. Sorting could not be implemented in the ODH (the sorting parameter provided by the API does not work..)
USED
- Detail.Title
- Detail.BaseText
- Detail.GetThereText
- ImageGallery.ImageDesc

NEVER USED
- Detail.Header
- Detail.Keywords
- Detail.MetaDesc
- Detail.IntroText
- Detail.MetaTitle
- Detail.SubHeader
- Detail.AdditionalText
- ImageGallery.ImageName
- ImageGallery.ImageTitle
